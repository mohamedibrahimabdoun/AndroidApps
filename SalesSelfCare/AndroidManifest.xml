<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mtn.sales.salescarepkg"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".SalesSelfCareActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".MainPageActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:name=".DataRegistrationActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <!-- android:configChanges="orientation" -->
        <activity
            android:label="@string/app_name"
            android:name=".ActivateSIMActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity
            android:label="@string/app_name"
            android:name=".ActActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity
            android:label="@string/app_name"
            android:name=".ActReportActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:name=".VTUActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:name=".TransferActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:name=".VTURepoActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />

        <!-- android:configChanges="orientation" -->

        <activity
            android:label="@string/app_name"
            android:name="PurchasingActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" />

        <service android:name="mtn.sales.salescarepkg.CheckingConnService" />

        <receiver android:name="mtn.sales.salescarepkg.BroadCastRec" >
            <intent-filter >
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.allfocus" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_PHONE_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

</manifest>